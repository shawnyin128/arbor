interface:
  display_name: "Release"
  short_description: "Checkpoint or finalize Arbor features"
  default_prompt: "Use $release as an internal Arbor checkpoint/finalization gate; show only concise release status unless debug review is requested."

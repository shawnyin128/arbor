interface:
  display_name: "Internal Intake"
  short_description: "Silently route Arbor workflow requests"
  default_prompt: "Use $intake only for Arbor runtime routing, simulation, or debug review; normal users should not invoke it directly."

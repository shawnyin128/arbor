__pycache__/
*.pyc
.coverage
.DS_Store
.pytest_cache/
.ruff_cache/
.mypy_cache/

# This repository uses the pattern it ships, so AGENTS.md, CLAUDE.md, and the
# agent-written notes under .arbor/ are committed. The machine-written
# .arbor/session.json is excluded by .arbor/.gitignore, which `arbor init` writes.

# Local design notes and validation evidence.
/docs/

# Local Claude Code state, including worktree experiments.
/.claude/
